# This project is a native Android app and does not require any runtime API keys.
#
# (Historical note: this file previously declared GEMINI_API_KEY, left over from an
# AI Studio template. No Gemini/LLM code exists in the app, so it has been removed.)
